# Xor imported function 2 (`talos.oom`)

Terminal OOM host operation.  It sets the Universal host's OOM marker and
produces the distinguished host trap.  It is reachable only from `func4`
after allocator failure; the well-formed two-byte entry precondition excludes
that path.  Host-transition complexity is constant.
