[package]
name = "rust_vec"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
borsh = { version = "1.8", default-features = false }
talos-stdio = { path = "../talos_stdio" }
